CbzMage azw conversion requires the MobiMetadata library. It expects to find the library 
project in a folder named "CbzMageExternal" outside of the CbzMage solution folder.

So create this folder and checkout https://github.com/ToofDerling/MobiMetadata.git there 
(as "MobiMetadata") and you should be good to go.